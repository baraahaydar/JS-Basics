function msg() {
    var fn=document.getElementById('first_number').Value;
    var sn=document.getElementById('second_number').Value;
    var fmsg = fn * sn ;
    alert(fn + " * " + sn + " = " + fmsg);
}